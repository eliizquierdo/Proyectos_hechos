`El siguiente es un diagrama de clases. Similar al que se utilizará más adelante en Sistemas de Bas`es de Datos 1.

 

`El rectángulo se divide en tres partes:`

`El siguiente es un diagrama de clases. Similar al que se utilizar en Sistemas de Bases de Datos 1.`

`• En la parte del medio se detallan los atributos:`

`• El símbolo de – significa que el atributo es private.`

`• Luego el nombre seguido por :`

`• Por último el tipo.`

`• En la parte inferior se detallan los métodos específicos.`

`• El símbolo de + significa que el método es public.`

`• Luego sigue la misma estructura que los atributos (nombre y tipo).`

> `**Nota**: Para todas las clases del curso desarrollaremos constructores, getters, setters y toString, es por ese motivo que no es necesario agregarlas en el diagrama.`

Para este ejercicio se pide desarrollar la clase **Articulo** respetando el diagrama, es decir, NO

pueden agregar nuevos atributos.

• **public double precioVenta():** El método devuelve el valor de precioCosto con un

incremento del 20%.

• **public String toString():** Devuelve transformado en String: código, nombre,

precioCosto y precioVenta() del producto.

En la clase **Main** deberá crear dos objetos de tipo Articulo, uno con todos los valores cargados

con el constructor específico, otro con los métodos setters. Puede cargar los atributos con valores

constantes, tal como se realizó en el teórico.